var tm_ep = "http://localhost"
var tm_debug = true;

function DEBUG(s) {
    if (tm_debug) {
        //var timeformat = d3.timeFormat("%Y-%m-%d %H:%M:%S");
        //var d = new Date();
        //var n = timeformat(d);

        //console.log("[" + n + "] " + s);
        console.log(s);
    }
}
